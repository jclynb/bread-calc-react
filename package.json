{
  "name": "bread-calc-react",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "gh-pages -d dist",
    "upload": "npm run build && npm run deploy"
  },
  "dependencies": {
    "@popperjs/core": "^2.10.1",
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "react-dropdown": "^1.9.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^1.0.0",
    "vite": "^2.5.4",
    "gh-pages": "^3.2.3"
  }
}
